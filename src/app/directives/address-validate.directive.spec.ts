import { AddressValidateDirective } from './address-validate.directive';

describe('AddressValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new AddressValidateDirective();
    expect(directive).toBeTruthy();
  });
});
