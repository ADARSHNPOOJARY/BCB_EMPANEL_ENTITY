import { MobileNoOnlyDirective } from './mobile-no-only.directive';

describe('MobileNoOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileNoOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
