import { EmailOnlyDirective } from './email-only.directive';

describe('EmailOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
