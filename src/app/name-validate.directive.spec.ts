import { NameValidateDirective } from './name-validate.directive';

describe('NameValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new NameValidateDirective();
    expect(directive).toBeTruthy();
  });
});
