import { RemarksValidateDirective } from './remarks-validate.directive';

describe('RemarksValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new RemarksValidateDirective();
    expect(directive).toBeTruthy();
  });
});
